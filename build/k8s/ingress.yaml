apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: inspr-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: "inspr.com"
    http:
      paths:
      - path: /
        backend:
          serviceName: insprd-svc
          servicePort: 80
