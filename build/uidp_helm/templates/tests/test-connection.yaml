apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "UIDProvider.fullname" . }}-test-connection"
  labels:
    {{- include "UIDProvider.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "UIDProvider.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
