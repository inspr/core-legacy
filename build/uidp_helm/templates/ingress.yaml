apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: {{ .Values.ingress.name }}
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: {{ .Values.ingress.host }}
    http:
      paths:
      - path: /newuser
        backend:
          serviceName: {{ .Values.service.uidpName }}
          servicePort: {{ .Values.service.port }}
      - path: /deleteuser
        backend:
          serviceName: {{ .Values.service.uidpName }}
          servicePort: {{ .Values.service.port }}
      - path: /updatepwd
        backend:
          serviceName: {{ .Values.service.uidpName }}
          servicePort: {{ .Values.service.port }}
      - path: /login
        backend:
          serviceName: {{ .Values.service.uidpName }}
          servicePort: {{ .Values.service.port }}
      - path: /refreshtoken
        backend:
          serviceName: {{ .Values.service.uidpName }}
          servicePort: {{ .Values.service.port }}
