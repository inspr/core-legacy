apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.uidpName }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 80
      targetPort: 9001
      name: client
  selector:
    {{- include "UIDProvider.selectorLabels" . | nindent 4 }}