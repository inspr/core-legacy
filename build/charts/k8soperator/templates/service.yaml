apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8soperator.fullname" . }}
  labels:
    {{- include "k8soperator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k8soperator.selectorLabels" . | nindent 4 }}
