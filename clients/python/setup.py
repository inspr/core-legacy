from setuptools import setup

setup(
    name="inspr",
    packages=[
        "inspr",
    ],
    requires=[
        "requests",
        "flask"
    ],
    install_requires=[
        "requests",
        "flask"
    ]
)
