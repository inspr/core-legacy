apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "teste.fullname" . }}-test-connection"
  labels:
    {{- include "teste.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "teste.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
