apiVersion: v1
kind: dapp

meta:
  name: primesexample
spec:
  aliases:
    filter.filterinput:
      target: primesch1
    filter.filteroutput:
      target: primesch2
    printer.printerinput:
      target: primesch2

  boundary:
    input:
      - primesch1
      - primesch2
    output:
      - primesch1
      - primesch2
