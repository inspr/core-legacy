apiVersion: v1
kind: dapp

meta:
  name: bench
spec:
  aliases:
    sender.sendch:
      resource: benchannel
    receiver.receivech:
      resource: benchannel
  boundary:
    input:
      - benchannel
    output:
      - benchannel
